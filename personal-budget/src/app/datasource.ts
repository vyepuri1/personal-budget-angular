
export type Datasource = {
  datasets: [
    {
      data: string[],
      backgroundColor: string[],
    }
  ],

  labels: string[]
}

export type Datasource1 = [{
  "labels": string,
  "value": string
}]
